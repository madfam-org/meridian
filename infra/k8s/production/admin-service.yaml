apiVersion: v1
kind: Service
metadata:
  name: meridian-admin
  namespace: meridian
  labels:
    app: meridian-admin
    app.kubernetes.io/part-of: madfam-platform
spec:
  type: ClusterIP
  selector:
    app: meridian-admin
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
