apiVersion: v1
kind: Service
metadata:
  name: meridian-web
  namespace: meridian
  labels:
    app: meridian-web
    app.kubernetes.io/part-of: madfam-platform
spec:
  type: ClusterIP
  selector:
    app: meridian-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
